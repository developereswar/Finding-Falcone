import React, { useEffect, Fragment } from "react";

const Planets = props => {
  useEffect(() => {});

  return (
    <Fragment>
      <ul>
        <li>Test</li>
      </ul>
    </Fragment>
  );
};

export default Planets;
